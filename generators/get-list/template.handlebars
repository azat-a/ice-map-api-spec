test(`{{uppercase method}} {{url}} должен возвращать {{capitalize type}}`, async () => {
  const { data } = await axios.{{lowercase method}}('{{url}}');
  expect(data).toEqual(expect.any({{capitalize type}}));
});
